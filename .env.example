# JWT
JWT_SECRET=change_this_to_a_long_random_secret

# OTP feature flag — set to "true" to enable email OTP login
OTP_ENABLED=false

# Resend (only needed when OTP_ENABLED=true)
RESEND_API_KEY=re_xxxxxxxxxxxx
RESEND_FROM=POS Water <noreply@yourdomain.com>
